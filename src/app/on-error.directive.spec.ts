import { OnErrorDirective } from './on-error.directive';

describe('OnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new OnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
